- @survey.errors.full_messages.each do |message|
  %p= message

= form_for(@survey) do |form|
  .field
    = form.label :name
    = form.text_field :name

  .field
    = form.label :body
    = form.text_area :body

  .actions
    = form.submit 'Submit'
